export enum ShapeFillType {
    fill = 'fill',
    stroke = 'stroke'
}

export enum DiagramMode {
    View = 0,
    Edit = 0
}

export enum ShapeType {
    Rect = "rect",
    Circle = "circle",
    Triangle = "triangle",
    Line = "line",
    Pencil = "pencil"
}
